apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 9896
      protocol: TCP
      targetPort: http
    - port: 9996
      targetPort: grpc
      protocol: TCP
      name: grpc
