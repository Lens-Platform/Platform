{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "shopper_service.fullname" . }}-redis
data:
  redis.conf: |
    maxmemory 500mb
    maxmemory-policy allkeys-lru
    save ""
    appendonly no
{{- end }}
